spring:
  config:
    import: optional:file:../.env[.properties]

  datasource:
    url: jdbc:postgresql://localhost:5433/kurkboard
    username: kurkboard
    password: ${DB_PASSWORD}

  flyway:
    enabled: true